[tool.poetry]
name = "scholar"
version = "0.1.0"
description = ""
authors = ["Kirill Gashkov <k.e.gashkov@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
